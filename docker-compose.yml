version: '3.8'

services:
  backend:
    build: 
      context: ./backend  
    container_name: meetsync_api
    ports:
      - "8081:8080"     
    environment:
      - API_PORT=8080
      - NODE_ENV=development
    restart: unless-stopped